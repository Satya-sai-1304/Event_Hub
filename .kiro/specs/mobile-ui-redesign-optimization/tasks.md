# Implementation Plan

- [x] 1. Write bug condition exploration tests
  - **Property 1: Bug Condition** - Mobile UI Layout Defects
  - **CRITICAL**: These tests MUST FAIL on unfixed code — failure confirms the bugs exist
  - **DO NOT attempt to fix the tests or the code when they fail**
  - **NOTE**: These tests encode the expected behavior — they will validate the fix when they pass after implementation
  - **GOAL**: Surface counterexamples that demonstrate each bug exists
  - **Scoped PBT Approach**: Scope each property to the concrete failing case(s) for reproducibility
  - Create `frontend/src/components/__tests__/mobile-ui-redesign.test.tsx`
  - **Test 1 — Duplicate Back Button** (`isBugCondition_DuplicateBackButton`):
    - Render `BrowseEventsPage` inside `DashboardLayout` at mobile viewport width (375px)
    - Assert that exactly 2 back button elements are present in the render tree (confirms bug on unfixed code)
    - Expected counterexample: two elements matching "Back" / `ArrowLeft` found simultaneously
  - **Test 2 — Navbar Safe-Area** (`isBugCondition_NavbarCutoff`):
    - Render `BottomNavBar` and assert `paddingBottom` equals `env(safe-area-inset-bottom)` on the outer `<nav>` and that height is 70px
    - Observe whether the inner flex container overrides or inherits the padding correctly
  - **Test 3 — Missing Card Buttons** (`isBugCondition_MissingCardButtons`):
    - Render `EventCard` with `showActions="customer"` and an upcoming event at 180px card width
    - Assert both "Book Now" and "View Details" buttons are present and visible
    - Expected counterexample: one or both buttons missing / collapsed on narrow viewport
  - **Test 4 — Explore Events Section** (`isBugCondition_ExploreEventsSection`):
    - Render `CustomerDashboard` and assert an element with text "Explore Events" IS present (confirms bug on unfixed code)
    - Expected counterexample: "Explore Events" heading found in render tree
  - **Test 5 — Oversized Gallery** (`isBugCondition_OversizedGallery`):
    - Render `EventGallery` at mobile viewport (375px) and assert image container height > 100px (confirms bug on unfixed code)
    - Expected counterexample: image containers with `aspect-video` class producing height > 100px
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct — it proves the bugs exist)
  - Document counterexamples found to understand root cause
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Buggy Render Paths Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (organizer/admin cards, desktop layout, lightbox, sold-out states)
  - **Observation targets**:
    - `EventCard` with `showActions="organizer"` — observe Edit, Delete, Notify, Cancel buttons render correctly
    - `EventCard` with `showActions="admin"` — observe Remove button renders correctly
    - `EventCard` with sold-out event (`isSoldOut=true`) — observe "Sold Out" badge and disabled button
    - `EventCard` with `status="completed"` — observe "Event Ended" indicator
    - `EventCard` with `status="cancelled"` — observe "Cancelled" indicator
    - `EventGallery` lightbox — observe Dialog opens, image navigation, like/download/share buttons
    - `BottomNavBar` at desktop viewport (1024px) — observe it is hidden (`block md:hidden`)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements in design
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.9_

- [x] 3. Fix mobile UI layout defects

  - [x] 3.1 Remove duplicate back button from BrowseEventsPage
    - In `frontend/src/pages/dashboard/BrowseEventsPage.tsx`, delete the `{window.history.length > 1 && (<Button variant="ghost" ... onClick={() => navigate(-1)}> <ArrowLeft .../> Back </Button>)}` block (lines ~324–328)
    - Remove `ArrowLeft` from the lucide-react import since it will no longer be used
    - The `DashboardLayout` `BackButton` already handles back navigation for all sub-pages
    - _Bug_Condition: isBugCondition_DuplicateBackButton(X) where X.page = "BrowseEventsPage" AND DashboardLayout also renders BackButton_
    - _Expected_Behavior: count(backButtons, result) = 1 — only DashboardLayout's BackButton remains_
    - _Preservation: Back button behavior on all other pages (non-BrowseEventsPage routes) is unchanged_
    - _Requirements: 2.1, 3.5_

  - [x] 3.2 Fix BottomNavBar safe-area padding and icon/label sizing
    - In `frontend/src/components/BottomNavBar.tsx`, verify `paddingBottom: "env(safe-area-inset-bottom)"` is on the outer `<nav>` element and is not overridden by the inner flex container
    - Ensure all nav items use `<Icon size={24}>` (22–24px) and label `fontSize: "11px"` with `marginTop: "4px"` consistently
    - Confirm `height: "70px"` is set on the outer `<nav>` and the inner div does not reduce effective height
    - Confirm `className="block md:hidden"` correctly suppresses the nav bar on desktop
    - _Bug_Condition: isBugCondition_NavbarCutoff(X) where X.device = "mobile" AND paddingBottom ≠ "env(safe-area-inset-bottom)"_
    - _Expected_Behavior: height = 70px, paddingBottom = "env(safe-area-inset-bottom)", icons 22–24px, label 11px_
    - _Preservation: BottomNavBar continues to render only for authenticated customers on the five main pages; hidden on desktop_
    - _Requirements: 2.2, 3.1, 3.4_

  - [x] 3.3 Enforce both action buttons always visible in EventCard customer view
    - In `frontend/src/components/EventCard.tsx`, in the customer action section for active events (not completed/cancelled/sold-out), ensure the button container uses `flex gap-1.5 w-full` with `flex-wrap: nowrap`
    - Ensure "View Details" button has explicit `h-8 px-3 text-xs` and "Book Now" button has `flex-1 h-8 text-xs`
    - No changes to conditional rendering logic for completed, cancelled, sold-out, organizer, or admin states
    - _Bug_Condition: isBugCondition_MissingCardButtons(X) where showActions="customer" AND eventStatus NOT IN ["completed","cancelled"] AND buttons collapse_
    - _Expected_Behavior: hasElement(result, "Book Now") AND hasElement(result, "View Details") always true for active customer events_
    - _Preservation: Organizer/admin/sold-out/completed/cancelled card states render identically to before_
    - _Requirements: 2.3, 3.2, 3.3, 3.6, 3.7_

  - [x] 3.4 Remove Explore Events section from CustomerDashboard
    - In `frontend/src/pages/dashboard/CustomerDashboard.tsx`, locate and delete the JSX block that renders the "Explore Events" section
    - Only "Featured Events" and "Recommended Events" sections should remain in the events area
    - All other sections (search bar, quick services, latest bookings, banners, pending ratings, popular vendors) remain unchanged
    - _Bug_Condition: isBugCondition_ExploreEventsSection(X) where X.page = "CustomerDashboard" AND "ExploreEvents" section is rendered_
    - _Expected_Behavior: NOT hasElement(result, "Explore Events") — section is absent from render tree_
    - _Preservation: Featured Events section continues to display real API data; Recommended Events section unchanged_
    - _Requirements: 2.4, 3.8_

  - [x] 3.5 Compact EventGallery to 3-column grid with constrained image heights
    - In `frontend/src/components/EventGallery.tsx`, replace `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` with `grid-cols-3 md:grid-cols-3 lg:grid-cols-4` on the gallery grid container
    - Replace `aspect-video` on the main image container with a fixed height class (`h-[90px]`) and ensure `object-fit: cover` is applied
    - Reduce `CardContent` padding to keep cards compact (e.g., `p-2` instead of `p-4`)
    - Do NOT modify the `Dialog` lightbox, `AnimatePresence`, navigation handlers, like/download/share buttons, or any lightbox state
    - _Bug_Condition: isBugCondition_OversizedGallery(X) where X.component = "EventGallery" AND X.viewport = "mobile" AND imageHeight > 100_
    - _Expected_Behavior: imageContainerHeight(result) <= 100, columnsPerRow(result) = 3, border-radius 8px, gap 6px_
    - _Preservation: Lightbox Dialog, image navigation, like/unlike, download, share all unchanged_
    - _Requirements: 2.5, 3.9_

  - [x] 3.6 Apply additional mobile UI polish (typography, spacing, card styling)
    - Apply consistent typography across dashboard and event pages: title `font-size: 16–18px`, subtitle `font-size: 13–14px`, small/meta text `font-size: 11–12px`
    - Apply `padding-left: 12px` and `padding-right: 12px` to content containers to prevent edge-to-edge stretching on mobile
    - Constrain event card image height to max 140px using `object-fit: cover` in `EventCard`
    - Apply 2-per-row event card grid on mobile: `grid-cols-2` with `gap: 8px` in browse/featured grids
    - Apply consistent section spacing: `margin-bottom: 16px` between sections, `margin-bottom: 10px` between cards
    - Apply card shadow `box-shadow: 0 2px 6px rgba(0,0,0,0.08)`, `border-radius: 12px`, `padding: 12px` to `EventCard`
    - Add active/tap effect on cards (e.g., `active:scale-[0.98]` or `active:opacity-80` transition)
    - _Bug_Condition: isBugCondition conditions 1.6–1.10 (oversized text, missing padding, excessive spacing, no tap effect)_
    - _Expected_Behavior: Requirements 2.6–2.12 satisfied — compact, polished mobile layout_
    - _Preservation: Desktop layouts, organizer/admin views, and all booking flows unchanged_
    - _Requirements: 2.6, 2.7, 2.8, 2.9, 2.10, 2.11, 2.12_

  - [x] 3.7 Verify bug condition exploration tests now pass
    - **Property 1: Expected Behavior** - Mobile UI Layout Defects Resolved
    - **IMPORTANT**: Re-run the SAME tests from task 1 — do NOT write new tests
    - The tests from task 1 encode the expected behavior
    - When these tests pass, it confirms the expected behavior is satisfied
    - Run all five bug condition tests from step 1
    - **EXPECTED OUTCOME**: All tests PASS (confirms all bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5 — Expected Behavior Properties from design_

  - [x] 3.8 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Buggy Render Paths Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 — do NOT write new tests
    - Run all preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm organizer/admin card views, desktop layout, lightbox, sold-out/completed/cancelled states all unchanged
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.9_

- [x] 4. Checkpoint — Ensure all tests pass
  - Run the full test suite: `npm test -- --run` (or `vitest --run`) from the `frontend` directory
  - Ensure all bug condition tests pass (bugs are fixed)
  - Ensure all preservation tests pass (no regressions)
  - Manually verify on a mobile viewport (375px): single back button on Browse Events, compact gallery grid, both card buttons visible, no Explore Events section, bottom nav with safe-area padding
  - Ask the user if any questions arise
